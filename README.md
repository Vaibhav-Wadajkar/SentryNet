# SentryNet – Network Intelligence Dashboard

WiFi device scanner with AI anomaly detection (Isolation Forest), live dashboard, CSV export, scan comparison, device notes, and ping latency.

## Requirements
- Python 3.8+
- Flask, scikit-learn, numpy, manuf

## Run
```bash
pip install flask numpy scikit-learn manuf
python scanner.py
